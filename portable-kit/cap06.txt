oriol@server:~$ sudo systemctl status nginx
● nginx.service - A high performance web server and a reverse proxy server
     Loaded: loaded (/lib/systemd/system/nginx.service; enabled; preset: enabled)
     Active: active (running) since Mon 2025-01-06 10:30:00 CET; 1h ago
       Docs: man:nginx(8)
   Main PID: 1024 (nginx)
      Tasks: 3 (limit: 2314)
     Memory: 8.5M
        CPU: 892ms
     CGroup: /system.slice/nginx.service
             ├─1024 nginx: master process /usr/sbin/nginx -g daemon on; master_process on;
             ├─1025 nginx: worker process
             └─1026 nginx: worker process

oriol@server:~$ sudo cat /etc/nginx/sites-available/default
server {
    listen 80;
    server_name exercici5.oriol.canillas.local;

    location / {
        root /var/www/exercici5;
        index index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:3000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

oriol@server:~$ sudo nginx -t
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
